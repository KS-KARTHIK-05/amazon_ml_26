# Filename compatibility note

The working Claude Code entry point is uppercase `CLAUDE.md`. It imports the
shared rules from `agent.md`. Read those files; this lowercase file contains no
separate rules and is not relied upon for automatic instruction discovery.
