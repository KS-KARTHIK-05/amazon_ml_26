# Repository agent entry point

Read `agent.md` for shared implementation rules and `PROJECT_STATUS.md` for the
current verified state. For implementation tasks, follow the relevant phase in
`docs/IMPLEMENTATION_SPEC.md` and the user's current scope.

`CLAUDE.md` imports `agent.md` for Claude Code. Keep shared rules in `agent.md`;
do not maintain competing copies here. Do not start modeling during a request
that only asks to prepare documentation or prompts.
